// Note: Twilio requires a publicly accessible URL (not localhost)
// For local development, use ngrok or similar: ngrok http 3001
// Then set WEBHOOK_URL=https://your-ngrok-url.ngrok.io/api/webhooks/twilio